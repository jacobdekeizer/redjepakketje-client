<?php

namespace jacobdekeizer\Endpoints;

use jacobdekeizer\Client;

class Base
{
    /**
     * @var Client
     */
    protected $client;

    /**
     * @param Client $client
     */
    public function __construct(Client $client)
    {
        $this->client = $client;
    }
}